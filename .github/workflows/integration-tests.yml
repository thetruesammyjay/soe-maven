name: Integration Tests CI

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'integration-tests/**'
      - 'pom.xml'
  pull_request:
  workflow_dispatch:
    branches: [ "main", "master" ]
    paths:
      - 'integration-tests/**'
      - 'pom.xml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Ensure Maven Wrapper is executable
      run: chmod +x mvnw
        
    - name: Verify integration-tests Codebase
      run: ./mvnw clean package -pl integration-tests -am -DskipTests
